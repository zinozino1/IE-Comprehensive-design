const searchBtn = document.querySelector("#anal-search");

const searchBtnHandler = function () {};

const init = function () {
    searchBtn.addEventListener("cilck", searchBtnHandler);
};

if (searchBtn) init();
